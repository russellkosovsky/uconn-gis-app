# Python deps for the temporal-GIS pipeline + API.
# Pinned to versions verified working on Python 3.13.4 (see DECISIONS.md for the
# 3.11 -> 3.13 note). osmnx is pinned because its features_from_bbox signature
# changed between 1.x and 2.x — this code targets the 2.x bbox-tuple form.

osmnx==2.1.0
geopandas==1.1.3
shapely==2.1.2
pandas==3.0.3
requests==2.34.2
rasterio==1.5.0          # installed for the (stubbed) USGS raster step; unused this session

# Backend
fastapi==0.138.0
uvicorn[standard]==0.49.0

# Tests
pytest==9.1.1
