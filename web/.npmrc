# This is an open-source project — install from the public npm registry.
# Overrides any private/authenticated registry in the user's global ~/.npmrc so
# `npm install` works without credentials.
registry=https://registry.npmjs.org/
